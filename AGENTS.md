# 承認なしで実行許可されるコマンド

以下のコマンドは実行を許可します。

- flutter analyze
-- ただし、libフォルダ以下のみに限定

# 実装と検証をセットで行う

コードの実装が完了したら、flutter analyze lib/ または dart analyze lib/を実行してエラーがないことを確認すること

# デバッグ用出力フォルダ

ログの書き出しなど、デバッグのためファイルへ書き出す必要がある場合は、ルート直下にあるtempフォルダを使用すること